paste(list.subdirs[i],list.filenames[n], sep = "/")
strtrim(list.filenames[n], nchar(list.filenames[n])-4) <-fread(paste(list.subdirs[i],list.filenames[n], sep = "/"))
strtrim(list.filenames[n], nchar(list.filenames[n])-4) <-fread(paste(list.subdirs[i],list.filenames[n]))
strtrim(list.filenames[n], nchar(list.filenames[n])-4) <-fread(paste(list.subdirs[i],list.filenames[n], sep = "/"))
paste(list.subdirs[i],list.filenames[n], sep = "/")
myfile = paste(list.subdirs[i],list.filenames[n], sep = "/")
DT = fread(myfile)
myfile
as.character(myfile)
DT = fread(as.character(myfile)
)
# =======================================================================================
# LOAD LIBRARIES
# =======================================================================================
library(data.table)
# =======================================================================================
# LOAD DATA
# =======================================================================================
# list all dirs in /data
list.subdirs = list.dirs(path = "./data/final")
# create a loop to read in your data from all subdirs of "./data/final" using fread
for (i in 2:length(list.subdirs))
{
# setwd
setwd(list.subdirs[i])
# create a list from these files
list.filenames<-list.files(path = list.subdirs[i], pattern=".txt")
# create a loop to read in your files using fread
for (n in 1:length(list.filenames))
{
strtrim(list.filenames[n], nchar(list.filenames[n])-4) <-fread(list.filenames[n])
}
}
# =======================================================================================
# LOAD LIBRARIES
# =======================================================================================
library(data.table)
# =======================================================================================
# LOAD DATA
# =======================================================================================
# list all dirs in /data
list.subdirs = list.dirs(path = "./data/final")
# create a loop to read in your data from all subdirs of "./data/final" using fread
for (i in 2:length(list.subdirs))
{
# setwd
setwd(list.subdirs[i])
# create a list from these files
list.filenames<-list.files(pattern=".txt")
# create a loop to read in your files using fread
for (n in 1:length(list.filenames))
{
strtrim(list.filenames[n], nchar(list.filenames[n])-4) <-fread(list.filenames[n])
}
}
# =======================================================================================
# LOAD LIBRARIES
# =======================================================================================
library(data.table)
# =======================================================================================
# LOAD DATA
# =======================================================================================
# list all dirs in /data
list.subdirs = list.dirs(path = "./data/final")
# create a loop to read in your data from all subdirs of "./data/final" using fread
for (i in 2:length(list.subdirs))
{
# setwd
setwd(list.subdirs[i])
# create a list from these files
list.filenames<-list.files(pattern=".txt")
# # create a loop to read in your files using fread
# for (n in 1:length(list.filenames))
# {
#   strtrim(list.filenames[n], nchar(list.filenames[n])-4) <-fread(list.filenames[n])
# }
}
setwd("~/R/DataScience-JohnsHopkins-Capstone")
# =======================================================================================
# LOAD LIBRARIES
# =======================================================================================
library(data.table)
# =======================================================================================
# LOAD DATA
# =======================================================================================
# list all dirs in /data
list.subdirs = list.dirs(path = "./data/final")
# create a loop to read in your data from all subdirs of "./data/final" using fread
for (i in 2:length(list.subdirs))
{
# setwd
setwd(list.subdirs[i])
# create a list from these files
list.filenames<-list.files(pattern=".txt")
# # create a loop to read in your files using fread
# for (n in 1:length(list.filenames))
# {
#   strtrim(list.filenames[n], nchar(list.filenames[n])-4) <-fread(list.filenames[n])
# }
}
setwd("~/R/DataScience-JohnsHopkins-Capstone")
getwd()
dir = "/home/ericandbeethoven/R/DataScience-JohnsHopkins-Capstone"
list.subdirs = list.dirs(path = dir + "/data/final")
# list all dirs in /data
dir = "/home/ericandbeethoven/R/DataScience-JohnsHopkins-Capstone"
list.subdirs = list.dirs(path = paste(dir, "/data/final"))
# list all dirs in /data
setwd("/home/ericandbeethoven/R/DataScience-JohnsHopkins-Capstone")
list.subdirs = list.dirs(path = "./data/final")
# =======================================================================================
# LOAD LIBRARIES
# =======================================================================================
library(data.table)
# =======================================================================================
# LOAD DATA
# =======================================================================================
# list all dirs in /data
setwd("/home/ericandbeethoven/R/DataScience-JohnsHopkins-Capstone")
list.subdirs = list.dirs(path = "./data/final")
# create a loop to read in your data from all subdirs of "./data/final" using fread
for (i in 2:length(list.subdirs))
{
# create a list from these files
list.filenames[i]<-list.files(path = list.subdirs[i], pattern=".txt")
# create a loop to read in your files using fread
# for (n in 1:length(list.filenames))
# {
#   strtrim(list.filenames[n], nchar(list.filenames[n])-4) <-fread(list.filenames[n])
# }
}
# =======================================================================================
# LOAD LIBRARIES
# =======================================================================================
library(data.table)
# =======================================================================================
# LOAD DATA
# =======================================================================================
# list all dirs in /data
setwd("/home/ericandbeethoven/R/DataScience-JohnsHopkins-Capstone")
list.subdirs = list.dirs(path = "./data/final")
list.filenames<- list()
# create a loop to read in your data from all subdirs of "./data/final" using fread
for (i in 2:length(list.subdirs))
{
# create a list from these files
list.filenames[i]<-list.files(path = list.subdirs[i], pattern=".txt")
# create a loop to read in your files using fread
# for (n in 1:length(list.filenames))
# {
#   strtrim(list.filenames[n], nchar(list.filenames[n])-4) <-fread(list.filenames[n])
# }
}
# =======================================================================================
# LOAD LIBRARIES
# =======================================================================================
library(data.table)
# =======================================================================================
# LOAD DATA
# =======================================================================================
# list all dirs in /data
setwd("/home/ericandbeethoven/R/DataScience-JohnsHopkins-Capstone")
list.subdirs = list.dirs(path = "./data/final")
list.filenames<- list()
# create a loop to read in your data from all subdirs of "./data/final" using fread
for (i in 2:length(list.subdirs))
{
# create a list from these files
list.filenames<-list.files(path = list.subdirs[i], pattern=".txt")
# create a loop to read in your files using fread
# for (n in 1:length(list.filenames))
# {
#   strtrim(list.filenames[n], nchar(list.filenames[n])-4) <-fread(list.filenames[n])
# }
}
# =======================================================================================
# LOAD LIBRARIES
# =======================================================================================
library(data.table)
# =======================================================================================
# LOAD DATA
# =======================================================================================
# list all dirs in /data
setwd("/home/ericandbeethoven/R/DataScience-JohnsHopkins-Capstone")
list.subdirs = list.dirs(path = "./data/final")
list.filenames<- list()
# create a loop to read in your data from all subdirs of "./data/final" using fread
for (i in 2:length(list.subdirs))
{
# create a list from these files
list.filenames<-list.files(path = list.subdirs[i], pattern=".txt")
# create a loop to read in your files using fread
for (n in 1:length(list.filenames))
{
myDT = strtrim(list.filenames[n], nchar(list.filenames[n])-4)
myFile = list.filenames[n]
# strtrim(list.filenames[n], nchar(list.filenames[n])-4) <-fread(list.filenames[n])
}
}
# =======================================================================================
# LOAD LIBRARIES
# =======================================================================================
library(data.table)
# =======================================================================================
# LOAD DATA
# =======================================================================================
# list all dirs in /data
myDir = "/home/ericandbeethoven/R/DataScience-JohnsHopkins-Capstone/data/final"
list.subdirs = list.dirs(path = myDir)
list.filenames<- list()
# create a loop to read in your data from all subdirs of "./data/final" using fread
for (i in 2:length(list.subdirs))
{
# create a list from these files
list.filenames<-list.files(path = list.subdirs[i], pattern=".txt")
# create a loop to read in your files using fread
for (n in 1:length(list.filenames))
{
myDT = strtrim(list.filenames[n], nchar(list.filenames[n])-4)
myFile = list.filenames[n]
# strtrim(list.filenames[n], nchar(list.filenames[n])-4) <-fread(list.filenames[n])
}
}
list.subdirs[2]
# =======================================================================================
# LOAD LIBRARIES
# =======================================================================================
library(data.table)
# =======================================================================================
# LOAD DATA
# =======================================================================================
# list all dirs in /data
myDir = "/home/ericandbeethoven/R/DataScience-JohnsHopkins-Capstone/data/final"
list.subdirs = list.dirs(path = myDir)
# create a loop to read in your data from all subdirs of "./data/final" using fread
for (i in 2:length(list.subdirs))
{
# create a list from these files
list.filenames<-list.files(path = list.subdirs[i], pattern=".txt")
# create a loop to read in your files using fread
for (n in 1:length(list.filenames))
{
myDT = strtrim(list.filenames[n], nchar(list.filenames[n])-4)
myFile = list.filenames[n]
RDIST = list.subdirs[i]
filename = 'grep RDIST myFile'
# strtrim(list.filenames[n], nchar(list.filenames[n])-4) <-fread(list.filenames[n])
}
}
# =======================================================================================
# LOAD LIBRARIES
# =======================================================================================
library(data.table)
# =======================================================================================
# LOAD DATA
# =======================================================================================
# list all dirs in /data
myDir = "/home/ericandbeethoven/R/DataScience-JohnsHopkins-Capstone/data/final"
list.subdirs = list.dirs(path = myDir)
# create a loop to read in your data from all subdirs of "./data/final" using fread
for (i in 2:length(list.subdirs))
{
# create a list from these files
list.filenames<-list.files(path = list.subdirs[i], pattern=".txt")
# create a loop to read in your files using fread
for (n in 1:length(list.filenames))
{
myDT = strtrim(list.filenames[n], nchar(list.filenames[n])-4)
myFile = list.filenames[n]
RDIST = list.subdirs[i]
filename = paste(RDIST, myFile, sep = "/", collapse = NULL)
# strtrim(list.filenames[n], nchar(list.filenames[n])-4) <-fread(list.filenames[n])
}
}
# =======================================================================================
# LOAD LIBRARIES
# =======================================================================================
library(data.table)
# =======================================================================================
# LOAD DATA
# =======================================================================================
# list all dirs in /data
myDir = "/home/ericandbeethoven/R/DataScience-JohnsHopkins-Capstone/data/final"
list.subdirs = list.dirs(path = myDir)
# create a loop to read in your data from all subdirs of "./data/final" using fread
for (i in 2:length(list.subdirs))
{
# create a list from these files
list.filenames<-list.files(path = list.subdirs[i], pattern=".txt")
# create a loop to read in your files using fread
for (n in 1:length(list.filenames))
{
myDT = strtrim(list.filenames[n], nchar(list.filenames[n])-4)
myFile = list.filenames[n]
RDIST = list.subdirs[i]
fname = paste(RDIST, myFile, sep = "/", collapse = NULL)
system.time(myDT <-fread(fname))
}
}
# =======================================================================================
# LOAD LIBRARIES
# =======================================================================================
library(data.table)
# =======================================================================================
# LOAD DATA
# =======================================================================================
# list all dirs in /data
myDir = "/home/ericandbeethoven/R/DataScience-JohnsHopkins-Capstone/data/final"
list.subdirs = list.dirs(path = myDir)
# create a loop to read in your data from all subdirs of "./data/final" using fread
for (i in 2:length(list.subdirs))
{
# create a list from these files
list.filenames<-list.files(path = list.subdirs[i], pattern=".txt")
# create a loop to read in your files using fread
for (n in 1:length(list.filenames))
{
fname = paste(list.subdirs[i], list.filenames[n], sep = "/", collapse = NULL)
system.time(strtrim(list.filenames[n], nchar(list.filenames[n])-4)  <-fread(paste('grep RDIST', fname), header=F, sep=","))
}
}
system.time(DT<-fread("/home/ericandbeethoven/R/DataScience-JohnsHopkins-Capstone/data/final/en_US/en_US.blogs.txt")
)
# =======================================================================================
# LOAD LIBRARIES
# =======================================================================================
library(data.table)
# =======================================================================================
# LOAD DATA
# =======================================================================================
# list all dirs in /data
myDir = "/home/ericandbeethoven/R/DataScience-JohnsHopkins-Capstone/data/final"
list.subdirs = list.dirs(path = myDir)
# create a loop to read in your data from all subdirs of "./data/final" using fread
for (i in 2:length(list.subdirs))
{
# create a list from these files
list.filenames<-list.files(path = list.subdirs[i], pattern=".txt")
# create a loop to read in your files using fread
for (n in 1:length(list.filenames))
{
fname = paste(list.subdirs[i], list.filenames[n], sep = "/", collapse = NULL)
system.time(strtrim(list.filenames[n], nchar(list.filenames[n])-4)  <-read.txt(fname, stringsAsFactors=FALSE ))
}
}
# =======================================================================================
# LOAD LIBRARIES
# =======================================================================================
library(tm)
# =======================================================================================
# LOAD DATA
# =======================================================================================
# list all dirs in /data
myDir = "/home/ericandbeethoven/R/DataScience-JohnsHopkins-Capstone/data/final"
list.subdirs = list.dirs(path = myDir)
# create a loop to read in your data from all subdirs of "./data/final" using fread
for (i in 2:length(list.subdirs))
{
# create a list from these files
list.filenames<-list.files(path = list.subdirs[i], pattern=".txt")
# create a loop to read in your files using fread
for (n in 1:length(list.filenames))
{
system.time(strtrim(list.filenames[n], nchar(list.filenames[n])-4)  <- system.file(list.subdirs[i], list.filenames[n], package = "tm" ))
}
}
# =======================================================================================
# LOAD LIBRARIES
# =======================================================================================
library(tm)
library(data.table)
# =======================================================================================
# LOAD DATA
# =======================================================================================
# list all dirs in /data
myDir = "/home/ericandbeethoven/R/DataScience-JohnsHopkins-Capstone/data/final"
list.subdirs = list.dirs(path = myDir)
# create a loop to read in your data from all subdirs of "./data/final" using fread
for (i in 2:length(list.subdirs))
{
# create a list from these files
list.filenames<-list.files(path = list.subdirs[i], pattern=".txt")
# create a loop to read in your files using fread
for (n in 1:length(list.filenames))
{
system.time(strtrim(list.filenames[n], nchar(list.filenames[n])-4)  <- system.file(list.subdirs[i], list.filenames[n], package = "tm" ))
}
}
?strtrim
# =======================================================================================
# LOAD LIBRARIES
# =======================================================================================
library(tm)
library(data.table)
# =======================================================================================
# LOAD DATA
# =======================================================================================
# list all dirs in /data
myDir = "/home/ericandbeethoven/R/DataScience-JohnsHopkins-Capstone/data/final"
list.subdirs = list.dirs(path = myDir)
# create a loop to read in your data from all subdirs of "./data/final" using fread
for (i in 2:length(list.subdirs))
{
# create a list from these files
list.filenames<-list.files(path = list.subdirs[i], pattern=".txt")
# create a loop to read in your files using fread
for (n in 1:length(list.filenames))
{
system.time(strtrim(list.filenames[n], nchar(list.filenames[n])-4) = system.file(list.subdirs[i], list.filenames[n], package = "tm" ))
}
}
# =======================================================================================
# LOAD LIBRARIES
# =======================================================================================
library(tm)
library(data.table)
# =======================================================================================
# LOAD DATA
# =======================================================================================
# list all dirs in /data
myDir = "/home/ericandbeethoven/R/DataScience-JohnsHopkins-Capstone/data/final"
list.subdirs = list.dirs(path = myDir)
# create a loop to read in your data from all subdirs of "./data/final" using fread
for (i in 2:length(list.subdirs))
{
# create a list from these files
list.filenames<-list.files(path = list.subdirs[i], pattern=".txt")
# create a loop to read in your files using fread
for (n in 1:length(list.filenames))
{
strtrim(list.filenames[n], nchar(list.filenames[n])-4) = system.file(list.subdirs[i], list.filenames[n], package = "tm" )
}
}
# =======================================================================================
# LOAD LIBRARIES
# =======================================================================================
library(tm)
library(data.table)
# =======================================================================================
# LOAD DATA
# =======================================================================================
# list all dirs in /data
myDir = "/home/ericandbeethoven/R/DataScience-JohnsHopkins-Capstone/data/final"
list.subdirs = list.dirs(path = myDir)
# create a loop to read in your data from all subdirs of "./data/final" using fread
for (i in 2:length(list.subdirs))
{
# create a list from these files
list.filenames<-list.files(path = list.subdirs[i], pattern=".txt")
# create a loop to read in your files using fread
for (n in 1:length(list.filenames))
{
myDT =  strtrim(list.filenames[n], nchar(list.filenames[n])-4)
myDir = list.subdirs[i]
myFile = list.filenames[n]
myDT = system.file(myDir, myFile, package = "tm" )
}
}
myDT = system.file(myDir, myFile, package = "tm" )
?system.file
# =======================================================================================
# LOAD LIBRARIES
# =======================================================================================
library(tm)
library(data.table)
# =======================================================================================
# LOAD DATA
# =======================================================================================
# list all dirs in /data
myDir = "/home/ericandbeethoven/R/DataScience-JohnsHopkins-Capstone/data/final"
list.subdirs = list.dirs(path = myDir)
# create a loop to read in your data from all subdirs of "./data/final" using fread
for (i in 2:length(list.subdirs))
{
# create a list from these files
list.filenames<-list.files(path = list.subdirs[i], pattern=".txt")
# create a loop to read in your files using fread
for (n in 1:length(list.filenames))
{
myDT =  strtrim(list.filenames[n], nchar(list.filenames[n])-4)
myDir = list.subdirs[i]
myFile = list.filenames[n]
myDT = system.file("texts", myDir, package = "tm" )
}
}
myDT = system.file("texts", myDir, package = "tm" )
?readPlain
# list all dirs in /data
setwd("/home/ericandbeethoven/R/DataScience-JohnsHopkins-Capstone/data/final")
list.subdirs = list.dirs(path = myDir)
setwd("/home/ericandbeethoven/R/DataScience-JohnsHopkins-Capstone/data/final")
list.subdirs = list.dirs()
# =======================================================================================
# LOAD LIBRARIES
# =======================================================================================
library(tm)
library(data.table)
# =======================================================================================
# LOAD DATA
# =======================================================================================
# initiate lists
list.subdirs = list()
list.filenames = list()
# list all dirs in /data
setwd("/home/ericandbeethoven/R/DataScience-JohnsHopkins-Capstone/data/final")
list.subdirs = list.dirs()
# create a loop to read in your data from all subdirs of "./data/final" using fread
for (i in 2:length(list.subdirs))
{
# create a list from these files
list.filenames[i]<-list.files(path = list.subdirs[i], pattern=".txt")
}
